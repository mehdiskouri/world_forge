---
name: "forge.connect"
version: "0.1.0"
description: "Traverse the bpy hypergraph: list boundaries, query containment / adjacency layers, inspect specific edges. Stage A placeholder."
triggers: ["how are these regions connected", "show the hypergraph", "list boundaries"]
requires_tools: ["forge.query_layer", "forge.list_boundaries", "forge.inspect_boundary"]
requires_subagent: false
---

# forge.connect (Phase 5 Stage A placeholder)

Full body lands in branch ``skills-visualize-cleanup-connect``.
