---
name: "forge.cleanup"
version: "0.1.0"
description: "Find orphaned specs, stale realizations, and conflicting locks; recommend manual cleanup steps. Stage A placeholder."
triggers: ["clean up", "remove orphans", "find stale realizations"]
requires_tools: ["forge.list_regions", "forge.inspect_spec", "forge.list_locks"]
requires_subagent: false
---

# forge.cleanup (Phase 5 Stage A placeholder)

Full body lands in branch ``skills-visualize-cleanup-connect``.
