"""Package marker for :mod:`forge_mcp.analyze`.

Houses pure-numpy analysis routines that consume generated heightmaps
and stream geometries and emit the structured analytics the agent
sees through ``forge.analyze_region`` and through the populated
``SpecRecord.summary`` field.
"""

from __future__ import annotations
