"""Host-side maintenance scripts (one-off utilities, not packaged).

These are exempt from the runtime test/coverage gate but must still pass
ruff and mypy --strict so they remain trustworthy when re-run.
"""
